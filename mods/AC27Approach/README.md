# AC27Approach

BepInEx 6 IL2CPP plugin for Airport Control 25 (Playtest) that live-patches aircraft in-game, driven entirely through the game's **native UDP command channel** (no overlay, no hotkeys). Implements the design in `docs/bepinex-aircraft-override-report.md` (sections 4–5.4, 6, 8).

- **Plugin ID**: `com.ac27.approach` v1.0.1
- **Input**: UDP only — the game's own `AircraftUdpCommandService` on `127.0.0.1:20267`
- **Three commands**:
  - `update_heading` — **heading-only** override (2026-08-03, decoupled): forces the aircraft's nose to a heading each tick; position and speed stay 100% the game's — the aircraft keeps flying its own route at the game's own speed. Speed is never read for control and never written. An optional rate field (`update_heading|CS|dx|dy|rate`) rotates the nose **smoothly** at that many °/s of GAME time (scaled with the game's speed multiplier, frozen while paused); omitted/≤0 = instant snap. (`update_position` is tolerated as a legacy alias with its kts field ignored.)
  - `clear_for_appr` — hand a STAR (state 30) aircraft onto the final approach (state 5), mirroring an ACL pre-spawned state=5 aircraft: speed channels always written, BOTH radio slots handed to the tower channel (v2, 2026-08-04), `_waitingForCommands = [PermitLanding]`, and since **v5 (2026-08-04) NO path is planted** — the game's own `ApproachState.Init` derives the approach from the aircraft's own `AppPointList` (its procedure from the IAF), the game's pre-capture steering naturally heads the aircraft there, and since **v11 (2026-08-05) the AVC drive** re-paces the aircraft at the commanded speed instead of the plan's (the plan-paced crawl — CCA4851; the v5–v10 plan-anchor reschedule is REMOVED — it could not change the model's pace, only collapse its ETA — see the v11 notes)
  - `altitude` — **climb/descend-and-maintain** override (2026-08-04): forces the aircraft's **Y only** toward a target altitude in feet; X/Z, heading, speed, and route stay 100% the game's — the aircraft keeps flying its own lateral path. Smooth by default (`Mathf.MoveTowards` at rate ft/min of GAME time — the same `GameDt`-scaled rule as the turn, frozen while paused); direction is implicit in the target (above current = climb, below = descend)
  - `update_speed` — **fly-speed override** (2026-08-04, v11 drive 2026-08-05, **v12 constant-acceleration ramp 2026-08-05, v13 1.5 kt/s default 2026-08-05**): transitions the aircraft to the commanded speed at a **constant acceleration** — the drive's own ramped state (`SpeedCurrentKts`, seeded from the actual speed at arm) is stepped toward the target at `AvcDriveAccelKtsPerSec` (1.5 kt/s of GAME time — 737-class: 180→240 kt in ~40 s, symmetric for decel — or the frame's `accel=N`) and written to every speed surface each tick (the Controller `targetSpeed` field + the `SpeedController` ramp output + the surfaces the game eases); persists until the tower-frequency handoff or a `clear_for_appr` supersedes it. The ONE command that touches speed — `update_heading`/`altitude` stay speed-orthogonal

## Requirements

- Airport Control 25 Playtest with BepInEx 6 IL2CPP installed (the editor's "BepInEx Debug Mode" tab can install it)
- The game must be running (the UDP server binds `127.0.0.1:20267` only while the game is up)
- Optional: the AC27 Level Editor for the `send-patch-command` bridge

## Install

The csproj deploys automatically on every build:

```
dotnet build mods/AC27Approach
```

copies `AC27Approach.dll` to:

```
<GameDir>\BepInEx\plugins\AC27Approach.dll
```

The GitHub release workflow builds the plugin in CI (`dotnet build ... -c Release -p:GameDir=... -p:SkipDeploy=true`) against the vendored build-time refs in `lib/BepInEx/{core,interop}` — compile-time dependencies only, never shipped; the release attaches just `AC27Approach.dll`. After the game updates, refresh the vendored refs with `sync-refs.ps1` and commit the changes.

Manual install = same thing: drop the DLL (with the rest of `BepInEx\plugins` untouched) and start the game. BepInEx 6 IL2CPP loads plugins from `BepInEx\plugins\` at startup — **fully restart the game** (not just a level reload) after dropping or replacing the plugin.

Verify load in `BepInEx\LogOutput.log`:

```
AC27Approach loaded
[AC27Approach] Aircraft.Step (postfix): applied
[AC27Approach] View hijack (Aircraft3D.SetWorldPosition): applied
[AC27Approach] UDP Mechanism A (AircraftUdpCommandService.ExecuteSelectAircraft): applied
[AC27Approach] UDP Mechanism B (AircraftUdpCommandService.FixedTick postfix): applied
[AC27Approach] UDP Mechanism B (Socket.Receive capture, 4-arg): applied
[AC27Approach] UDP Mechanism B (Socket.Receive capture, 1-arg): applied
[AC27Approach] UDP log suppression (AircraftUdpCommandService.LogBadDatagramOnce): applied
[AC27Approach] Level reset (AircraftUdpCommandService.Start postfix): applied
[AC27Approach] Level reset (AircraftUdpCommandService.Dispose prefix): applied
[AC27Approach] Dynamics.RestoreRuntimeData (trace): applied
[AC27Approach] Dynamics.SetCurrentState (trace): applied
```

(`AircraftDynamicsData.DynamicsParams` is deliberately NOT in this list — its setter is an IL2CPP **field accessor**: the IL2CPP detour backend refuses it and the managed fallback fails ("Parameter 'value' not found"), and the game's native C++ writes the field directly, never through the managed stub. Re-plant detection is a per-step pointer diff in the plugin's tick instead: `params-replant: <CS> DynamicsParams ← <class>` — the line is commented out since the log audit 2026-08-05, see the Notes bullet.)

These are the **runtime-verified** hook points (2026-08-03). The two obvious candidates are deliberately NOT patched — they apply cleanly at load but crash per-frame at runtime in this IL2CPP context:

- `Execute(in UdpCommand)` — the `in`-byref binding NREs inside the Harmony trampoline (`NullReferenceException` in the prefix)
- `TryParse(ReadOnlySpan<byte>, out UdpCommand)` — ref-struct param makes the Harmony DMD invalid IL (`InvalidProgramException` on **every** frame, including plain selects — it breaks the game's own UDP parsing)

Mechanism A hooks `ExecuteSelectAircraft(string)` (plain string param — called for every parsed SelectAircraft command). Mechanism B reads the datagram back from the service's `_receiveBuffer` in a `FixedTick()` postfix, with a `Socket.Receive` postfix as an alternative capture. The content dedup (`_lastHandledFrame`) applies to the **FixedTick path only** — it exists to skip the stale buffer re-read every tick when no new datagram arrived; the `Socket.Receive` path dispatches every real datagram unconditionally (a byte-identical re-send — e.g. re-applying the same command after a level restart — is still a real command), and the dedup is cleared at level load (see Level restarts below). **Interop gotcha:** Il2CppInterop stubs expose private fields as public *properties* with the same name — `Traverse.Field("_receiveBuffer")` resolves nothing and silently no-ops; the plugin reads `svc._receiveBuffer` directly. Each dispatched command also logs a confirmation line, e.g. `[AC27Approach] patch: update_heading → CSN9355 (1,0): applied (Mechanism B)`.

## Usage

### Mechanism A — hijacked SelectAircraft frame (always available)

Standard SelectAircraft frame (CommandId `1`) whose callsign field starts with `!`. The game's own server parses it; the plugin intercepts it before selection happens, so **no aircraft gets selected**.

```
!5:<callsign>        → clear_for_appr (callsign field is 12 bytes — fits any callsign)
```

From the editor devtools:

```js
await electronAPI.sendUdpCommand(1, '!5:' + callSign);
```

### Mechanism B — extended frame (full command set)

Custom CommandId `0x00E7` + pipe-delimited ASCII payload:

| Command | Payload | Notes |
|---|---|---|
| `update_heading` | `update_heading\|CS\|dx\|dy[\|rate]` | **Heading-only**: (dx, dy) = world direction components — heading H → (dx, dy) = (sin H, cos H), +Z = north, +X = east (180 = `0,-1`, 270 = `-1,0`, 360 = `0,1`). No speed field: the plugin never touches speed, position, or route on this frame (the `update_speed` command is the exception — see its row) — the aircraft points at the heading while the game's dynamics keeps flying it. Optional 5th field `rate` = smooth-turn rate in °/s of GAME time (2026-08-03): the nose rotates toward the heading at that rate, stepped by the game's OWN clock (`GameTime.TimeScale` × `GameTime.FixedDeltaTime` — the true per-tick advanced game time: 1/60 at ×1, 1/6 at ×10, 0 while paused → frozen with the game; **not** `Time.timeScale`, which reads 1 at any game speed — v7, live-verified CES5578; v7 correction: `GameTime.Delta` read 0.00000 live — stubbed getter — and froze every rotation in the v6 build, so it is diagnostic-only now) and frozen while paused; omitted or ≤0 = INSTANT (pre-smoothing behavior). Legacy `update_position\|CS\|dx\|dy[\|kts]` parses the same way (kts validated but ignored, one-time deprecation note — it is never a rate). Zero direction = no heading command (the game's own heading passes through) — it does NOT clear an override (there is no clear path; the override ends via `clear_for_appr` or a level switch — explicitly reset at level load, see Level restarts) |
| `clear_for_appr` | `clear_for_appr\|CS[\|kts][\|appr][\|native=0][\|rate=N][\|accel=N]` | `kts` optional — approach speed in raw knots (omitted/0 = the ACL default 240 — ALWAYS written, v3); `appr` optional — named approach procedure (omitted = nearest APP route to the aircraft); `native=0` skips `CommandContinueApproach` (its deferred radio flow ~3 s is the suspected post-patch actor — the direct fires alone produce the same transition); `rate=N` (v6, 2026-08-04) = the bounded de-snap's nose rate in °/s of GAME time — the nose rotates at that rate toward the IAF (the game's own pre-capture steering aim) while the aircraft is pre-capture, then releases on capture (the game's steering flies the final turn at the IAF); omitted = the plugin's 3°/s standard default; `accel=N` (v12, 2026-08-05) = the pre-capture speed-lift's ramp rate in kt of GAME time per second (omitted = the plugin's 1.5 kt/s default, v13 — the lift ramps from the aircraft's actual speed, never snaps). Keyed scan (not positional): a numeric field is always `kts`, the first other field is the procedure name, `native=0`/`rate=N`/`accel=N` are flags in any position — `rate=3` as a bare field would be misread as a 3 kt approach speed |
| `track` | `track\|CS` | Toggle the 1 s parameter trace for one callsign (diagnostics): every second the plugin dumps the aircraft's full params — aircraft state (Fly/Approach), dynamics state, the `DynamicsParams` object (whichever class, with path lists summarized), the **active state machine state with ITS OWN path copies** (`st=ApproachState stPath=… stPr=…` — the list the game's path-following actually reads, which the data channel does NOT reflect after an `Init`-capture) — plus position/altitude/heading/speed/route/waiting commands. `clear_for_appr` arms a ~3.3 s per-step watch (20 `watch: …` lines at step resolution — 200 steps, one line per 10 steps) so the post-patch seconds land in the log without the auto-trace stream (the 30 s auto-track was removed 2026-08-03 v2 — `track|CS` is the deliberate 1 s dump path) |
| `altitude` | `altitude\|CS\|targetFt[\|rateFpm]` | **Climb/descend-and-maintain** (2026-08-04): forces the aircraft's Y toward `targetFt` (feet) at `rateFpm` ft/GAME-minute — smooth, the same `GameDt`-scaled fixed-tick interpolation as the heading turn (`Mathf.MoveTowards` at `rate × dt/60` per tick; frozen while paused, ×2 speed doubles the per-tick step so the move completes in the same GAME time); rateFpm omitted or ≤0 = the plugin default `DefaultAltRateFpm` (1000 ft/min). Conversion: 1 GU = 100 m → `ft = y × 100/0.3048` (≈ ×328.084; sanity: Y = 15.24 GU = 5000 ft — the game's approach ceiling). `targetFt` ≤ 0 or NaN is invalid → `override: <CS> altitude REJECTED …`. Only Y is overridden — model `Position.y` + `HeightFeet` (the game readout, written in ft) each tick, and the `Aircraft3D.SetWorldPosition` channel lock re-points the view's Y (the visible transform's last writer); X/Z come fresh from the game's own dynamics write every tick. An altitude hold ends via `clear_for_appr` (the handoff drops the override — the aircraft descends the glideslope) or a level switch (explicitly reset at level load, see Level restarts); it is ORTHOGONAL to a heading override and a fly-speed override (an `update_heading`/`update_speed` preserves an active altitude hold and vice versa — the Entry is mutated in place) |
| `update_speed` | `update_speed\|CS\|kts[\|accel=N]` | **Fly-speed override** (2026-08-04; the per-tick mechanism is the **v11 AVC drive**, 2026-08-05; **v12 constant-acceleration ramp**, 2026-08-05): `kts` = raw knots (int; the editor slider range 180-240, thumb = live telemetry speed — raw knots, no scaling); optional 4th field `accel=N` = the ramp rate in kt of GAME time per second (omitted/≤0 = the plugin default `AvcDriveAccelKtsPerSec` 1.5, v13 — a bare numeric 4th field is REJECTED: the kts contract is the 3rd field only). Positional 3-part parse — the 3rd field is ALWAYS kts (unlike cfa's keyed scan). At arm: the data-channel speed fields are seeded with the aircraft's ACTUAL speed + the ACL accel constants (`PositiveTaxiAcceleration` 1 / `NegativeTaxiAcceleration` −2) + the reactives (v12 — before v12 the arm snapped the channel to kts). Per tick (`DriveAvcSpeed`): the plugin's OWN ramped state `SpeedCurrentKts` (seeded from the actual speed at arm; a re-command continues from the ramp state) steps toward `kts` at the ramp rate × `GameDt()` (frozen on pause, ×N at ×N game speed), and that ONE value is written to every speed surface — the AVC Controller's `targetSpeed` FIELD (the game writes the field directly, never `SetTargetSpeed` — the v10 probe silence), the `SpeedController` ramp output `speed` (direct write — the ramp IS `SpeedCurrentKts`), the ramp's own `targetSpeed`, and a re-assert of every surface the game eases toward its own Vapp (~144 kt) each tick. Stored state, never a surface re-read: the game eases surfaces back toward its own target between our ticks, and re-reading a clamped surface would stall the ramp. The v10 `SetTargetSpeed`-only + per-tick-`RescheduleEta` design is GONE — the plan-anchor shift could not change the model's pace (rem/ETA are end-anchored) and its ETA-collapse cascade triggered the `IncreaseDecelerate` that produced the 144 ease (see the v11 note). The override PERSISTS (no end command) and ends only when `clear_for_appr` supersedes it (its dispatch removes the override entry) or the aircraft's radio slot flips to the tower frequency (`speed: <CS> override dropped (radio → TWR)` — Type check vs `EChannel.Tower`, covering both the cfa handoff's TWR write and the game's own touchdown handoff). `kts ≤ 0` disarms defensively (the UI never sends it). Heading/altitude are ORTHOGONAL — `update_heading`/`altitude` do NOT clear it and vice versa |

The game's own parse cannot be stopped from running first (the postfix reads the buffer *after* it), so a stock game logs one `Aircraft UDP command ... dropped (UnknownCommand)` warning per frame — the plugin suppresses that specific warning via a `LogBadDatagramOnce` prefix; other bad-datagram reasons still surface.

From the editor devtools:

```js
// hand a STAR aircraft onto the final approach — v5 natural flow (v6):
// the aircraft heads for the IAF of its own procedure at the commanded
// speed (240 kt; the flight model's ETA is re-anchored to match — plan +
// the _appRouteTime floor — and the AVC target speed is lifted while
// pre-capture); rate=3 = the bounded de-snap: the nose rotates at 3°/s of
// GAME time toward the IAF until capture, then the game's steering flies
// the final turn
await electronAPI.sendPatchCommand({ type: 'clear_for_appr', callSign, rate: 3 });

// same, but the approach speed is commanded at 200 kt (kts omitted = the ACL default 240 — always written)
await electronAPI.sendPatchCommand({ type: 'clear_for_appr', callSign, kts: 200 });

// heading-only override: point the nose at 180° (the aircraft keeps flying
// its own route at the game's own speed — speed/position are never touched).
// rate = smooth turn at 3°/s of game time (omitted = instant snap)
await electronAPI.sendPatchCommand({ type: 'update_heading', callSign, dx: 0, dy: -1, rate: 3 });

// diagnostics: dump CSC6918's params every 1 s (toggle; send again to stop)
await electronAPI.sendPatchCommand({ type: 'track', callSign: 'CSC6918' });

// climb/descend-and-maintain: force the aircraft's Y toward 5000 ft at
// 1000 ft/min of GAME time (rate omitted = the same plugin default; only Y
// is overridden — X/Z, heading, speed and route stay the game's)
await electronAPI.sendPatchCommand({ type: 'altitude', callSign, targetFt: 5000, rate: 1000 });

// fly-speed override: hold the aircraft at 200 kt. The AVC drive ramps it
// at 1.5 kt/s of game time (v13 default; accel=N to override); re-asserted
// every tick until the tower handoff or a clear_for_appr supersedes it
// (no end command)
await electronAPI.sendPatchCommand({ type: 'update_speed', callSign, kts: 200 });
```

### Frame format (both mechanisms)

```
offset 0   uint32 LE  magic    0x43544147 "GATC" (1129595207)
offset 4   ushort    version   1
offset 6   ushort    commandId 1 (Mechanism A) | 0x00E7 (Mechanism B)
offset 8   payload   Mechanism A: 12-byte NUL-padded ASCII callsign
                     Mechanism B: ASCII, pipe-delimited, NUL-padded to
                     exactly 64 bytes (frame = 72 bytes total)
```

The 64-byte NUL-padded payload field is a hard contract: the plugin reads the datagram back out of the game's receive buffer after the tick, so it finds the payload by scanning for the first NUL at offset 8 — variable-length frames would get stale buffer bytes appended. The editor already speaks the header byte-for-byte (`electron/udp_listener.js`); `send-patch-command` builds the 0x00E7 variant with the padded field. Never route `!` frames through `select-aircraft-in-map` (selection side-effects).

## Level restarts

The plugin lives in the game process, so an **in-game level restart** (game stays up) does not re-run `Load()` — all process-lifetime static state survives. Since v1.0.1 the plugin resets that state itself at level load, so a level restart is no longer a silent kill switch for the patch channel:

- **Triggers** (either fires `level reset: …` once per load — coalesced): the game's `AircraftUdpCommandService` is a per-level VContainer service — `Start()` (channel (re)bound) and `Dispose()` (channel torn down) postfix/prefix; plus the `Dynamics.RestoreRuntimeData` **burst** backstop (≥ 2 first-time restore calls within 1 s = the per-load restore signature, e.g. `level reset: Dynamics.RestoreRuntimeData burst (level load)`).
- **What resets**: all override entries (pre-restart overrides are dropped by design — the aircraft objects they key died with the level), the object-keyed reverse-lookup/probe/watch dictionaries, the `AirwayRouteService` cache (the stale-cache bug that made `clear_for_appr` throw after the first restart), `GameClock`, and the frame dedup.
- **Re-sends always dispatch**: the dedup that previously swallowed byte-identical re-sent frames (same callsign, same values — deterministic frames) is cleared at level load AND no longer applies to the `Socket.Receive` capture path at all — a genuinely re-received datagram is always a real command.
- If a `level reset:` line is missing after a level load, the burst detector did not fire — check the log; on load N+1 the `_restoreLogged` clear re-arms the burst (a second load of the same level still logs the full first-call list).

## Notes

- **Log audit (2026-08-05):** the per-update (per-tick) diagnostics are commented out in `OverrideController.cs` — each site is marked `// log audit 2026-08-05` — to stop the LogOutput flood. Disabled: `cfa: <CS> de-snap diag: …` (every 10th de-snap tick), `speedcorr: <CS> before|after …` (every 10th speed-override tick), `avc-probe:` / `sc-probe:` (game `SetTargetSpeed` writes), `params-replant: …` (re-plant pointer diffs), and the per-update `diag:` line (first 30 override steps). Event logs are unchanged: `patch:`, `override:`/`speed:` apply lines, the cfa cascade (`de-snap armed`, state check, radio handoff), drops (`de-snap dropped/released`, `speed: … dropped`), the 3.3 s `watch:` burst, the post-release observer, `trace:` (opt-in), startup. Re-enable any disabled line by uncommenting its marked site (then `dotnet build mods/AC27Approach` + full game restart).
- `clear_for_appr` semantics: only STAR aircraft (`EAircraftState.Fly`) can be handed off; a heading override is dropped at command time (it would fight the approach); `_waitingForCommands` is set to `[PermitLanding]` — the aircraft awaits landing clearance, exactly like an ACL state-5 spawn. **v5 natural flow (2026-08-04, CCA4851 — the overhaul):** the handoff NO LONGER plants any path. The game's own `ApproachState.Init` derives the approach path from the aircraft's own `AppPointList` (its procedure from the IAF — `cfa: <CS> approach path = the aircraft's own procedure (AppPointList, len=N)`; `GetRoute` `AirwayNodes` is the logged fallback when AppPointList is unavailable), and the game's pre-capture steering naturally heads the aircraft toward the IAF when the transition lands — no command-time anchor, nothing to backtrack to. The v2-v4 join-leg machinery (prepend, per-tick `path[0]` tracking, release rule, 6b path rewrite — history below) is REMOVED; the 6b state check is verify-only (`path ok (aircraft's own procedure)` — no rewriting, the aircraft's own procedure is correct by definition) and the `FlyApproachState` gate-bypass carries own-procedure params. **v6 (2026-08-04, live CJX2697 — the crawl AND the fast turn):** the natural cruise needs two more fixes. (1) **The ETA floor:** `AircraftFlightMetrics.ETA` = max(planDelta, `_appRouteTime` − elapsedSinceRouteEntry) — the cached `_appRouteTime` (the schedule-derived approach duration; live `afmAppT` 00:08:16.347, frozen all watch) floored the ETA at ~496 s even after v1's plan-anchor shift (ETA moved 1017 s → 496 s and stopped), so the pace stayed ~3 u/s. The reschedule is now **post-Init (6b2, `cfa: <CS> flight-model reschedule v2: …`)** and shifts BOTH the plan anchor (`SetEndTime(Arrival)`, raw ticks) AND `_appRouteTime` (the interop `Il2CppSystem.TimeSpan` field) by the same −(ETA − remaining/speed) → ETA = remaining/speed → the pace target = the commanded speed; an `ETA > newEta + 5 s` guard makes it ~idempotent. **v7 (2026-08-04, live CCA4851 — the rebuild clobber):** the dispatch-time shift alone is NOT enough — the transition finalize rebuilds the flight model ONCE from the level schedule (afm `_shouldUpdateMetaData` → `Update()` re-derives the plan + `_appRouteTime`; live: the AFTER dump read `afmAppT=00:00:11.7`, watch step 0 back at `00:07:55.867` with `afmRem` 1446.6 → 1614.2), so the reschedule is now a shared `RescheduleEta(ac, kts, tag)` helper called at 6b2 (`dispatch`) AND per-tick from the de-snap branch (`per-tick`) — the guard makes it fire once after the rebuild and stay silent from there (log spam-capped: first 3 fires, then every 30th). (2) **The stale AVC speed target:** the operative speed is `Dynamics.AVCController.SetTargetSpeed` (absolute knots) — the approach state leaves the pre-handoff STAR pace (~2.4 kt) in place until capture, so the aircraft crawled at exactly the STAR pace (1.24 u/s = rem 1256 / ETA 1017) for the whole watch even with the channel at 240; the de-snap re-asserts the commanded knots every tick while pre-capture. **v11 (2026-08-05): that re-assert is now the AVC drive (`DriveAvcSpeed`) — v10 showed `SetTargetSpeed` alone did NOT stick: the game writes the controller's `targetSpeed` field directly (zero `avc-probe` hits) and the `SpeedController` ramp stayed pinned at the crawl. The drive writes the field, the ramp output `speed` (5 kt/s of GAME time) and the eased surfaces per tick (see the `update_speed` row/note). **v12 (2026-08-05): the lift is now a constant-acceleration ramp** — the drive's stored `SpeedCurrentKts` (seeded from the aircraft's ACTUAL speed at the command — the command-time channel write is seeded too, no 1-tick 240 blip) climbs to the commanded knots at the frame's `accel=N` (default `AvcDriveAccelKtsPerSec` 1.5 kt/s of GAME time, v13); re-pacing is gradual by design (a crawl 2.4→240 lift takes ~2.6 min — see the watch signature below). (3) **The bounded de-snap (`rate=N` honored again):** the game's own pre-capture easing sweeps the nose at ~35°/s (0.59°/step — the fast-turn report); at 6b (only when the transition confirmed `ApproachState` — a still-STAR aircraft is never fought) the plugin arms a de-snap entry: each tick the nose rotates at the frame's rate (3°/s default, GAME time) toward the IAF — which IS the game's own pre-capture steering aim (zero fight) — and releases on capture (dist to path[0] < 120 u — the game's tangent steering then flies the final turn at the IAF) or the cap — v9 removed the on-aim release (`cfa: <CS> de-snap released (captured|cap)`; see the v9 note below). Watch signature to expect (v13): NO `flight-model reschedule v2` lines at all (the reschedule is REMOVED — the drive re-paces the model, see the v11 notes below), `stPr` = 0.000 during the pre-capture cruise to the IAF (EXPECTED — not a stall), `afmETA` ≈ 12 s at the commanded pace, `rbVel` climbing at the LIFT RAMP — 2.4→~7 kt within the watch's first 3.3 s at ×1 (1.5 kt/s — EXPECTED, NOT a stall; ≈ 123 u/s only after the ~2.6 min lift completes), nose rotating ≈ 0.05°/step at ×1 (3°/s — the de-snap), `de-snap released (captured)` around dist ≈ 120 u — **~55 s in for the crawl case** (the ramp-speed closure, well inside the 36k-tick/600 s cap; ~5 s only when the aircraft is already near the commanded speed), then the natural final turn at 240 kt. **v8-d (2026-08-04, CSN2197 — post-release observation, diagnostic only):** the user reported "sometimes aircraft overshoot the turning direction, and have to turn back" — but the log ends at the `de-snap released` line (the release fired at ~tick 400, AFTER the 200-step watch ended) so the game-owned post-release behavior is UNOBSERVED — no fix is designed against an assumed mechanism. Instrumentation added (no behavior change): the game's true steering output is stashed per tick (`CommandedDirection` — the channel-lock prefixes already passed it) and logged every 10 ticks during the hold (`cfa: <CS> de-snap diag: hdg …° aim(IAF) …° game …° Δ …° dist …` — Δ = angle between our aim and the game's output: the sweep evidence; **commented out since the log audit 2026-08-05**, see the Notes bullet); a 300-step (5 s) post-release observer logs the game-owned nose every 10 steps after a release (`cfa: <CS> post-release: hdg …° pos (…) rbVel …`); and `GameDt()` logs once when the clock resolution scans fail (`game-time: clock NOT resolved — fallback dt=…`) — the 21:08 log had no clock line at all, yet the rotation math (0.146°/tick ≈ 3°/s × 1/60 × 3) shows the fallback's `Time.timeScale` read 3 at game ×3, contradicting the earlier CES5578 "timeScale reads 1 at any speed" claim — the next test must show which clock path ran. **v9 (2026-08-04 — the overshoot fix, from the 21:36 post-release evidence):** the v8-d instrumentation caught the overshoot live (CCA4851 at ×1): released `(on-aim)` at hdg 253.3° (Δ 2.0° vs the IAF bearing 251.7°), still ~700 u out — then the game's own controller swung the nose LEFT at ~23°/s (its max rate, 0.39°/tick) for ~0.7 s, overshooting the IAF bearing to **232.5° — 21° past the aim** — and returned toward 251.7° with a first-order decay (τ ≈ 5 s, rate ≈ 0.2 × error): the user's "overshoot the turning direction, have to turn back". The de-snap diag showed `game` == `hdg` exactly (0.1°) through the whole hold — the game's steering TRACKS our write while clamped; its capture target is invisible until freed. Geometry check: the swing target ≈ the aircraft's perpendicular foot on the path's first-segment line extended backward (measured foot bearing 231.1° vs the observed trough 232.5°) — on re-engagement the game executes a **line-capture sequence** (max-rate turn to the foot, then the path-following target — the IAF — pulls it back with τ≈5 s), and it fires whenever the aircraft is handed over while OFF the path line. The on-aim release handed over at 700 u — 6 s out. **Fix:** the de-snap releases ONLY on capture (dist < 120 u — the original v6 join-window design) or the cap; the on-aim release is REMOVED, the cap is extended 600 → 36000 ticks (10 min at ×1 — must cover the crawl-speed closure: 700 u at the broken-STAR pace 1.24 u/s = 33.8k ticks at ×1, 11.4k at ×3), and a state guard drops the override immediately when the aircraft leaves the approach state (`cfa: <CS> de-snap dropped (state → …)` — necessary now that the cap is 60× longer). The de-snap holds the IAF bearing for the whole pre-capture cruise. The residual at the 120 u join window is UNVERIFIED (the foot is ~112 u away — the capture should be small or proximity-scaled): the post-release observer stays armed — the next log must show `post-release: hdg ≈ 251.7°` after a `(captured)` release (no 21° swing); tighten `CfaJoinDist` (120 → 50/30) if a visible transient remains. The clock line finally landed: `clock resolved — TimeScale 1.0` at ×1 ✓ (the ×3 value still to be seen in a fresh session). Also observed (out of scope at the time): the v7 speed lift did NOT take — rbVel 1.24 u/s at ×1 with afmETA 14.1 s (the crawl persists; at ×3 it's the 3.68 u/s of the 21:08 case) — **RESOLVED in v11 (the AVC drive; see the `update_speed` note).** (Pre-v5) The handoff also overwrote the aircraft's runtime `FlyApproachPathPointList` with the planted approach path — the aircraft's OWN procedure with the aircraft's position PREPENDED as the join leg. **The join leg kills the dead cruise (2026-08-04):** the approach state holds `pr` at 0 until the aircraft captures path[0], and the pre-join-leg plant put path[0] 300-700 units from mid-STAR aircraft (GetRoute's nearest-first-fix variant pick) — minutes of silent cruise (live log 2026-08-03: CES5578/CSN2197 `stPr=0.000` frozen through the watch; CSN2197's 60 s phase-2 bound with the tangent never swept). Starting the path AT the aircraft makes the approach activate immediately, so the game's path-following continues onto the ILS rather than the STAR's tail. **Join-leg v2 (2026-08-04, live log CSN2197):** the game's own `ApproachState.Init` (run synchronously during the transition fires) re-derives its approach path from `AppPointList` and mutated the planted params' `PathPointList` IN PLACE back to the plain 5-pt APP list (path[0] 890 units away; pointer unchanged, so the replant diff stays silent) — re-stalling the capture gate (the ~10 s dead hold, `stPr=0.000`, tangent never sweeping → cfa-turn's 60 s phase-2 bound) and producing the 180° turn-back to the stale join point once the game's steering finally engaged. Three fixes: (1) step 4d plants the join leg into `AppPointList` (channel + fly-state copies) BEFORE the fires, so Init derives join-leg geometry natively (`cfa: <CS> AppPointList overwritten (join leg, N pts)`; a re-command strips the previous join leg by pointer identity — `previous join leg stripped`); (2) the 6b state check builds a FRESH `ApproachDynamicsParams` (never reuses the in-place-mutated object) and re-plants channel + `_runtimeData`, plus rewrites the Init-derived fields `_initialPosition`/`_approachDirection`/`_touchDownPosition`/`startingProgress` (log: `… ApproachState path + Init fields rewritten`, `… ApproachState._runtimeData → fresh join-leg params`, `… params-replant: DynamicsParams ← fresh ApproachDynamicsParams`); (3) join-leg tracking (`TrackJoinLeg` in `OnAircraftStep`, v4 release rule 2026-08-04) keeps path[0] + `_initialPosition` + runtime/channel path[0] under the aircraft while it is still on the STAR in the deferred activation window — the aircraft drifts off a command-time path[0], and a stale path[0] is exactly what the game steers BACK to. When the approach transition LANDS (the first `ApproachState` tick) the join leg is pinned to the aircraft's position ONE final time and tracking STOPS: keeping the per-tick pin any longer is a self-referential trap — the gate holds PR=0 until the aircraft captures path[0], and a path[0] that chases the aircraft can never be "passed", so the capture never completes and the aircraft sits in the game's pre-capture steering at ~1.2 u/s (live log 2026-08-04 CCA4851: `rbVel 1.24` with `chTs=240`, `stPr` frozen at 0.004 for all 190 watch steps, tangent never sweeping → 60 s phase-2 bound). Released, the aircraft sits ON a static path[0], the gate opens, and pr advances at the game's own rate. New trace fields: `stInit=`, `stSP=` (`startingProgress`), `stA0=<2D>/<3D>`, `rtA0=<2D>/<3D>` (aircraft→path[0] distance, 2-D and 3-D — the gate-target discriminator: stA0 near 0/15 with rtA0 large = the gate reads the stale runtime-data path), `afmRem=`/`afmAppT=` (diagnostic only — `AircraftFlightMetrics` is Init-built from the flight plan, not rebuildable from the plugin). **Interop gotcha (live-verified 2026-08-03):** `DynamicsData.DynamicsParams` is typed `IDynamicsParams`, and Il2CppInterop wraps the interface-typed getter's return in the interface's interop class — `GetType().Name` is literally `IDynamicsParams` and `is FlyApproachDynamicsParams` **never matches** (the original fly-path overwrite silently no-oped). The concrete class is identified by its native class pointer (`Il2CppObjectBase.ObjectClass` vs `Il2CppClassPointerStore<T>.NativeClassPtr`) and the object re-wrapped via `Pointer`. The transition fires the game's own `CommandContinueApproach()` first (the canonical "continue approach" handoff, which respects the `FlyToApproachCondition` gate on the aircraft machine's Fly→Approach transition), then the direct `Fire(EAircraftTrigger.Approach)` and `FlyApproach2Approach()` as logged fallbacks. Live test (2026-08-03): all three fire, the game plays the `AtcContinueApproach` ATC audio, and the AFTER dump shows `Appr(5)`/`Approaching` with `route=RNAV ILS Z Rwy 19` `wait=[22]` — the state update now triggers. The first `Dynamics.RestoreRuntimeData()` call was traced to a LEVEL LOAD (native caller, before the patch — the managed stack showed only the postfix, so the caller is C++); no restore call fired after the patch in the logged window. **The remaining suspect (live data, 2026-08-03):** the state machine's ACTIVE state holds its own captured copies — `ApproachState._runtimeData` / `_pathPointList` (and `FlyApproachState._flyApproachPathPointList` / `_appPointList`), `Init`-copied when the state was activated at level load. The game's path-following (`ApproachState.Update`) reads THOSE, not the aircraft's data channel — so even with the data channel showing our planted `ApproachDynamicsParams`, the aircraft may keep flying the STAR's captured path. The tracer now exposes the active state's own list (`st=… stPath=… stPr=…`), the trace line includes `dataSame=1|0` (is `ac.DynamicsData` the same object the dynamics' state machine reads?), `Dynamics.SetCurrentState(IDynamicState, IDynamicsParams)` is hooked (every transition incl. a revert, with WHICH params object is activated), and the plugin's per-step tick diffs the data channel's `DynamicsParams` pointer against the one it planted (`params-replant: …` — catches the game re-planting the STAR params; the setter itself is an IL2CPP field accessor that neither Harmony backend can patch, and native writes bypass the managed stub anyway). The patch also REWRITES the state's own copies when they mismatch our path (step 6b state check: `cfa: … state check: ApproachState stPath=… — MISMATCH — rewriting`), plus the active `FlyApproachState._flyApproachPathPointList` (step 4b) so a revert also continues onto the ILS path. Every attempt is logged (`cfa: …`) and the patch emits `trace: … [BEFORE]` / `[AFTER]` param dumps at command time and arms the ~3.3 s per-step `watch:` — the state that actually stuck is visible in the log. (The 30 s auto-track was removed 2026-08-03 v2 — it flooded the log with 30 `trace:` lines per handoff; `track|CS` remains for deliberate 1 s dumps.) **Live test 2 (2026-08-03):** the replant check's first kill — `params-replant: … DynamicsParams ← FlyApproachDynamicsParams` at watch step 0, a FRESH object (new pointer, `pr=0.682` — the state's own progress) replacing our planted `ApproachDynamicsParams` within one step: the ACTIVE state owns the channel's params and snapshots its own state each step, so the class on the channel always follows the active state. The transition had NOT taken at the state-object level — `state check: STILL FlyApproachState` and `st=FlyApproachState` through all 90 watch steps — while the ENUM flipped (`dyn=Approaching`): a half-transition. Root cause: the `FlyToApproachCondition` gate (the aircraft was mid-STAR at pr≈0.68, and the step 4c fly-path overwrite removed the STAR tail the gate's position check anchors to, so the gate could never pass; the fires' transition was dropped upstream of `SetCurrentState`; the aircraft then flew a degenerate FlyApproachState — stall, then south-east drift). Fix (step 6b fallback): when the state check still finds `FlyApproachState`, the patch bypasses the gate via the game's canonical entry — `dyn.SetCurrentState(_approachState, plantedParams)` using the dynamics' pre-created `ApproachState` instance (nothing minted; wrapped in the `IDynamicState` interop class — the same interface-proxy gotcha), re-verifies the active state, rewrites its captured copies on mismatch, and if `SetCurrentState` itself refuses, force-writes `_currentState` directly (last resort, logged). The `DynamicsState` enum write moved AFTER the transition attempts (step 6c) — the pre-set enum produced the half-transition readback (AFTER dump showed `Appr(5)`/`Approaching` while the state object was still `FlyApproachState`). No `restore:` call fired post-patch in this window — `RestoreRuntimeData` did not act this time. An optional `kts` field commands the approach speed in raw knots (the game's own `ApproachSpeedKts` scale); **the patch ALWAYS writes the approach speed now (v3, 2026-08-04)** — `TaxiSpeed`/`TargetTaxiSpeed`/`DynamicsTargetTaxiSpeed` = kts or the ACL default 240 (log `cfa: <CS> speed: ts=… tts=… dtts=… fwd=True accel 1/-2`). The pre-v3 kts-only behavior left the fields untouched and the approach path-following crawled at ~1-4 u/s instead of ~123 u/s at 240 kt (live log 2026-08-03: `stPr` advanced 0.005/s — the aircraft crept along the STAR tail for minutes). The tracer's `chTs=`/`chTts=`/`chDTts=`/`chFwd=` fields show the channel values in every dump — the crawl diagnostic. **v4 (2026-08-04): the channel values alone are NOT proof of motion** — the live log showed every channel at 240 while the rigidbody crept at 1.24 u/s (`rbVel 1.24` ≈ 240 kt ÷ 100, `dynVel 0.00`): the aircraft was stuck in the game's pre-capture steering because the tracker's per-tick pin kept the capture gate shut (see the join-leg tracking note). The trace now also shows the ACTUAL motion and the dynamics-level speed fields every dump: `rbVel=` (Aircraft3D rigidbody velocity magnitude), `dynVel=` (`Dynamics.Velocity`), `dynTs=`/`dynDTts=`/`dynAir=` (the `Dynamics`-level speed fields — the game's own surface, distinct from the channel), plus `afmAdj=`/`afmETA=` (the flight model's `SpeedAdjustment`/`ETA` — the §8.17 "next suspect" once `chTs=240` is visible). **Radio handoff (v2, 2026-08-04):** the patch also resolves the airport's tower channel — `RadioChannelManager.GetResolvedChannel(EChannel.Tower)` via the VContainer `LifetimeScope` scan, with the `RadioSystem._radioChannelBindings` dictionary (PK → binding; the private-field-as-public-property gotcha) as fallback — and writes **BOTH** `_radioChannel` and `_jurisdictionRadioChannel` to it (`cfa: <CS> radio: radio <pk> → TWR(<pk>)` and `cfa: <CS> radio: jurisdiction <pk> → TWR(<pk>)`). v1 wrote only the jurisdiction slot and the aircraft stayed on the approach frequency — the tower seat still could not own it — v2 flips both, so the strip/telemetry move to the tower seat at command time (ACL parity stores the tower channel in BOTH slots). The tracer's `rc=`/`jrc=` fields (channel Type/PK) show both slots in every dump (BEFORE/AFTER, watch, manual `track|CS`) — the re-assert detector: a `rc`/`jrc` flip back names the culprit flow via the log line before it. Resolution failure is a logged skip (`cfa: … radio: tower channel NOT resolved …`) — the aircraft stays on approach and the game's touchdown auto-handoff (`ArrivalAircraftAutoContactTowerCondition`) self-heals as before.
- `update_heading` (heading-only, 2026-08-03): the game keeps full control of position, speed, route, and collisions — the plugin writes ONLY the heading channels (`Direction`, `DirectionReactive`, `Rotation`) each fixed tick, and the `set_Direction` channel lock re-points the game's own per-tick direction write (the dynamics' path-tangent heading). Speed channels are never written and never read for control (a display-only read appeared in the per-update `diag:` line — commented out since the log audit 2026-08-05, see the Notes bullet) — except via `update_speed`, the one command that commands speed. Each apply logs a before/after line: `override: <CS> before hdg …° spd … kt → after hdg …° rate …°/s` (heading in the game UI's convention, `atan2(dir.x, dir.z)`; +Z = north, +X = east). The aircraft points at the commanded heading but continues flying its original route — that is the intended behavior.
- `update_speed` (fly-speed override, 2026-08-04): the speed sibling of the heading override — the game's own speed write each Step (the AVC controller's target) is re-pointed, never fought from a distance. At arm the plugin writes the data-channel speed fields (`TaxiSpeed`/`TargetTaxiSpeed`/`DynamicsTargetTaxiSpeed` = kts, `ForwardSpeed = true`, accel 1/−2 — the ACL constants, so the aircraft's own integration ramps between speeds over in-game ticks) plus `Aircraft.TaxiSpeed`/`AirSpeedKnot` reactives, and one `RescheduleEta` (plan anchor + `_appRouteTime` → the model's pace target = the command). Per tick while active: `Dynamics.AVCController.SetTargetSpeed(TargetKts)` — the OPERATIVE speed target — and a self-converging per-tick `RescheduleEta` (the 5 s guard keeps it silent once converged; the log-spam cap bounds worst case). Each apply logs `speed: <CS> before N kt → after N kt (arm: ts/tts/dtts=N accel 1/-2, reschedule done)`. **No end command by design** (2026-08-04, user decision): the override persists and ends only when the aircraft's radio slot flips to the tower frequency (`speed: <CS> override dropped (radio → TWR)` — Type vs `EChannel.Tower`, per-tick, covering both the cfa handoff's TWR write and the game's own touchdown handoff) or a `clear_for_appr` supersedes it (its dispatch removes the override entry). A state guard drops it if the aircraft leaves the flight states (`speed: <CS> override dropped (state → …)`). Orthogonal to heading/altitude — the Entry is mutated in place. **v10 (2026-08-05, debug build — the speed never sticks, always settles at ~144 kt):** diagnostic-only instrumentation, no behavior change — the operative speed surface for the current state was UNKNOWN (the log showed the armed value honored nowhere: `AirSpeedKnot` readback eased 240 → 144 = the game's own plan target for the leg, actual motion stayed at the ~1-2 u/s crawl even after the reschedule collapsed the model ETA to 0.2 s). New per-tick evidence while `SpeedFollow` is active (commented out since the log audit 2026-08-05 — see the Notes bullet): `speedcorr: <CS> before|after …` every 10 ticks for the first ~300 (BEFORE = the game's end-of-tick state at postfix start — a surface already back at the game's own number proves a game-side clobber; AFTER = post-our-writes) with `spd`/`air` (the display reactives), `dynTs`/`dynDTts`/`dynAir` (the Dynamics-level fields), `afmRem`/`afmEta`/`pace` (the model's implied pace = rem ÷ ETA → kt), and `pace` (the measured displacement pace over the 10-tick window — the ground truth). The diag line carries the same block: `state=` (dyn state + Fly/Appr — the crawl's missing discriminator), `afmRem`/`afmEta`/`pace`/`adj` (`SpeedAdjustment` — report §8.17's named suspect), `dynTs`/`dynDTts`/`dynAir`. Plus an AVC probe (commented out since the log audit 2026-08-05 — see the Notes bullet): `AVCController.SetTargetSpeed` postfix (resolved via the `Dynamics.AVCController` property type; `avc-probe: <CS> game wrote SetTargetSpeed N kt (armed M kt)` — our own re-asserts write exactly the armed value and are filtered, so a hit is BY DEFINITION a game-side write of a different target, with its exact value and cadence). Expected log shape to correlate: the surface whose value tracks the `pace` column IS what the game flies. **v11 (2026-08-05 — root cause found and fixed):** the v10 session's log (and the zero `avc-probe` hits) closed the chain. (1) The reschedule CANNOT change the model's pace — `AircraftFlightMetrics.RemainingDistance`/`ETA` are end-anchored (rem ≈ pace × ETA with the pace baked in at Init, the stale STAR pace): the −1020 s dispatch shift read `ETA 1033 → 12.3 s` but `rem 1276 → 15 u` in the same tick (15 ≈ 12.3 × 1.23), and the per-tick re-assert (recomputing `newEta` from the collapsed rem, no floor) cascaded ETA to 0.1 s, rem → 0. (2) ETA ≈ 0 flipped `UpdateSpeedAdjustment` → `GetSpeedAdjustment_OnTime` to `IncreaseDecelerate` ("way ahead of plan"); the game's per-tick speed target is then the `DynamicCurveAsset.approachSpeedCurve` terminal at progress→1 — the A330-300's Vapp ≈ **144 kt** (game DATA, no literal 144 in the plugin) — and the readbacks ease to it. (3) Actual motion never accelerates because the game's per-tick AVC target (≈ the model pace, ~2.4 kt) is written to the controller's `targetSpeed` FIELD directly — never via `SetTargetSpeed` (hence probe silence) — pinning the `SpeedController` ramp at the crawl. **Fix — the AVC drive (`DriveAvcSpeed`, per tick, shared with the cfa de-snap):** write the Controller `targetSpeed` field, ramp the `SpeedController.speed` output (MoveTowards at `AvcDriveAccelKtsPerSec` = 5 kt/s of GAME time), set the ramp's own `targetSpeed`, and re-assert the Dynamics-level fields + channel block + reactives every tick (previously arm-only — the 144 ease reversed each tick). With motion at the commanded pace the model's rem/ETA recompute consistently (rem ≈ ETA × pace) and the game's own curve-derived target is the commanded speed mid-approach — the loop unwinds. ALL `RescheduleEta` callers removed (the function is dormant, guarded: min-rem + never-shift-below-target). New `sc-probe` (`SpeedController.SetTargetSpeed` postfix — the suspected real writer; commented out since the log audit 2026-08-05) and the speedcorr line's new `avcTgt`/`scSpd`/`scTgt` columns (kt). Expected healthy log: `speed: … (arm: ts/tts/dtts=240 accel 1/-2, avc drive armed)`; then `speedcorr after` samples all at 240, `scSpd` climbing 2→240, measured `pace` 2→240 kt (Δu 0.17 → ≈ 20.6 u per 10 ticks), `afmRem`/`afmEta` CONSISTENT (rem ≈ 1276, ETA ≈ 12 s — no collapse), `adj` never `IncreaseDecelerate`, `rbVel` ≈ 123 u/s at 240 kt. Near the threshold the curve still pulls Vapp — the natural end conditions (radio→TWR; cfa capture) end the drive there. **v12 (2026-08-05 — the constant-acceleration ramp, from the user's "the change is instant" report):** the drive no longer snaps — the per-entry `SpeedCurrentKts` (the plugin's OWN stored pace state, the speed sibling of the heading `Current` / altitude `AltCurrentFt` smoothing) is seeded from the aircraft's ACTUAL speed at arm (`patchSpeed` reads `ac.AirSpeedKnot`; the cfa command-time block seeds the same way — no 1-tick 240 blip, and the no-de-snap deep-failure path lifts via the game's own accel constants from the actual speed), stepped toward `TargetKts` at `AvcDriveAccelKtsPerSec` (1.5 kt/s, v13 — the user's pick: a 737-class 180→240 kt delta in ~40 s; the v12 5 kt/s was "way too fast"; symmetric, so decel uses the same rate — or the frame's `accel=N`) × `GameDt()` — frozen on pause, ×N at ×N speed — and that ONE value is written to every speed surface each tick (`sc.speed` is a DIRECT write now; the per-surface MoveTowards is gone — the ramp IS `SpeedCurrentKts`). Stored state, never a surface re-read: the game eases surfaces back toward its own target between our ticks, and re-reading a clamped surface would stall the ramp (the v10 pinned-ramp observation). A re-command (`update_speed|CS|180` mid-climb) continues from the ramp state — constant DECELERATION at the same rate, no jump; the arm log shows the seed and the rate: `(arm: ts/tts/dtts=… accel 1/-2, ramp <seed>→<kts> at <rate> kt/s, avc drive armed)` (the printed rate is the applied ramp — 1.5 kt/s default, or the frame's `accel=N`). `speedcorr` gains a `ramp` column before `armed` — the healthy v12 shape (the lines are commented out since the log audit 2026-08-05, see the Notes bullet; the tracer's `watch:`/`track|CS` dumps still carry the channel/dynamics fields `chTs=`/`dynTs=`/`dynAir=`/`rbVel=`, but the AVC readbacks `avcTgt=`/`scSpd=`/`scTgt=` and measured `pace` are speedcorr-only): `after` surfaces all ≈ the `ramp` value (climbing), measured `pace` tracks the ramp, `ramp` N→armed, afmRem/afmEta consistent, `adj` never `IncreaseDecelerate`. The frame gains the optional keyed 4th field `accel=N` (`update_speed|CS|240|accel=20` — a bare numeric 4th is REJECTED; the kts contract is the 3rd field only). A 2.4→240 kt lift at 1.5 kt/s takes ~2.6 min of GAME time — the cfa pre-capture cruise lengthens accordingly (release ≈ 55 s in for a crawl case, well inside the 36k-tick/600 s cap).
- `altitude` (2026-08-04): the vertical sibling of the heading override — the game's own Y write each Step (the glideslope/path altitude) is re-pointed, never fought from a distance. Write channels per tick: `Aircraft.Position` (Y only — X/Z fresh from the game's own dynamics write this Step), `Aircraft.PositionReactive`, `Aircraft.HeightFeet` (the game's ft readout — the value the game may drive its UI from), plus the `Aircraft3D.SetWorldPosition` channel lock for the visible view. The smoothed intermediate (`AltCurrentFt`) is what the channels receive — never the raw target (same reason as `CommandedDirection` — the game's own glideslope write would otherwise snap the aircraft to the full command every tick). Each apply logs `override: <CS> before alt … ft → after alt … ft rate … ft/min (X/Z + heading stay the game's)`; the (commented-out since the log audit 2026-08-05) diag line appended `altCur …ft altTgt …ft` while active, and the 1 s tracer's `pos` line still carries `alt …ft`. The game's own Y was visible in the diag's `dynPos` — the "untouched lateral motion" proof (via the tracer's `pos` now). A `clear_for_appr` drops the hold (the aircraft descends the glideslope); there is no standalone clear path (mirror heading).
- **Smooth turn (2026-08-03, dt source v7 2026-08-04):** with `rate > 0` the nose no longer snaps — each Entry carries a `Current` (smoothed intermediate heading) stepped toward the command with `Vector3.RotateTowards` at `rate × dt` per tick, where `dt` = `GameTime.FixedDeltaTime` (a STATIC constant — 1/60) × `GameTime.TimeScale` — the game's OWN clock service (`ContextCross.Clock.GameTime`, resolved lazily via `AircraftFlightMetrics._gameTime`, VContainer `TryResolve` fallback), the true per-tick advanced game time: 1/60 at ×1, 1/6 at ×10, 0 while paused (`IsPaused` gate — though `Aircraft.Step` may not even fire while paused) → the turn completes in the same GAME time at any speed multiplier and freezes with the game. **Not** `Time.timeScale`: live ×10 testing (the CES5578 116°-reversal slow-turn report — 0.048°/step = the unscaled 3°/s) showed `Time.timeScale` reads 1 at any game speed; the multiplier lives in `GameTime` (the UDP telemetry header mirrors it — `timeScale` offset 33, `simFlags` bit 0 = isPaused; the in-process clock is authoritative). **v7 correction (live CCA4851/CXA5172):** `GameTime.Delta` is a computed property with a stubbed getter — it read 0.00000 s/tick with the sim running and FROZE every rotation in the v6 build (update_heading noses held at the start heading; the cfa de-snap held 277° for the whole watch and fought the game's steering until the 600-tick cap). The clock-resolve log line prints Delta/TimeScale/FixedDeltaTime/IsPaused — at ×10 it must read `TimeScale 10.0`. `rate <= 0` (or omitted) = instant — `Current` is seeded at the command so the instant path is the pre-smoothing write verbatim. `Current` seeds from the aircraft's REAL heading at patch time (the turn starts where the nose points); a mid-turn re-command keeps `Current` and re-targets `Direction` (rotation continues from the intermediate heading). `CommandedDirection` returns `Current` so both direction-lock prefixes feed the smoothed value — otherwise the game's own path-tangent write would snap the nose back to the full command every tick.
- **Clear-for-approach is smooth too (2026-08-03, v2 2026-08-03; v5 REMOVED the machinery; **v6 2026-08-04 re-adds it in the bounded-de-snap shape** — kept below as the design record):** the v6 de-snap is the rate-limited handoff turn in its simplest form: armed at 6b only when the transition confirmed `ApproachState`; each tick the entry's target is the game's OWN pre-capture aim (the bearing to path[0] — the IAF), so nothing is fought; the nose chases it at the frame's rate (default 3°/s of GAME time via `GameDt`) and releases on capture (dist < 120 u) or the cap — v9 removed the on-aim release and the 10 s cap → 36k (see the v9 note); the game's steering then flies the final turn at the IAF. The v2/v3 Phase-2 machinery below (FollowGameHeading lock, sweep gate, 60 s bound) was the pre-v5 shape: the handoff's one-frame snap — the state transition + path overwrite makes the game write the approach path-tangent heading verbatim — is smoothed the same way. `clearForApproach` plants a `FollowGameHeading` entry instead of removing the override: the rotation TARGET is the game's OWN intended heading, stashed by the channel-lock prefixes (`SetDirectionPrefix` is the only place the true path-tangent heading is visible — the postfix's own write-back through the same setter is filtered by an exact-match rule, or it would stall the rotation). The lock is PHASE-GATED: Phase 1 = pass-through (the nose flies the STAR freely until the handoff ACTUALLY lands — the deferred `CommandContinueApproach` flow takes ~3 s; locking early would fight the STAR's own turns), Phase 2 = rotate onto the approach course at the rate once the aircraft leaves `Fly` state. **The drop is SWEEP-GATED (v2, drop rule v3 2026-08-04):** the game's own approach turn is also deferred — for ~3 s after the lock the tangent is still the STAR heading sitting ON the nose, and the original "converged" drop fired against it within ~2 ticks; the game's real turn (observed ~42°/s easing) then snapped the nose onto the final course. A drop now requires the tangent to have SWEPT > 2° from the lock-time snapshot (proof the deferred turn ran) AND the nose to have reached the final approach course — the planted path's LAST segment (`ApproachDirection`, where the game's tangent settles). v2's catch-the-live-tangent test fired ~2° into the sweep (the suppressed writes slow the game's easing to ~the nose's rate, so the nose caught it mid-turn) and the game's own easing then swept the nose across the rest of the turn at full rate (live log CXA5172: converged at tangent swept 2°, then hdg 102→160° at ~0.45°/tick — the "too fast" report). v3: the nose chases the game's live tangent at the rate the WHOLE way and releases within 1° of the final course — the game's own writes resume on the course, nothing snaps. Release also on the restore-revert back to `Fly`, after 10 s with no transition, or the 60 s phase-2 bound (backstop — residual gap logs). New log lines: `override: <CS> cfa-turn: nose …° → smooth turn armed …`, `… approach transition landed — rotating …`, `… game's approach turn running — tangent swept …° — nose chasing …`, `… cfa-turn converged … (tangent swept …°) … — override dropped`, `… back on the STAR (handoff reverted) — override dropped`, `… no approach transition within 10 s — override dropped`, `… 60 s phase-2 bound — override dropped`. The editor's composer sends the same `TURN_RATE_DEG_S` (3) on the cfa frame as a KEYED field (`rate=3`) — a bare numeric would be misread as the kts approach speed; frames without a rate use the plugin's `ClearForApprTurnRateDeg` default (same 3°/s).
- **Runway resolution (2026-08-03):** the assigned runway (`Aircraft.RunwayReactive.CurrentValue`) is NOT guaranteed to be a key in `AirwayRouteService.RouteDict` — that dictionary keys only runways with routes registered, and a mid-STAR aircraft can hold a runway that isn't one (live: `clear_for_appr` on CDG8288 threw `KeyNotFoundException` from `GetRoute`'s raw `RouteDict[runway]` access, surfacing as `patch: clear_for_appr → CDG8288 FAILED: Il2CppException`). `GetArrivalRunway` now resolves every runway against `RouteDict.Keys` (identity or `Name`/`PhysicalName` match via `FindKeyedRunway`) so the service always receives one of its own instances, and the `GetRoute` call is wrapped in a try/catch that logs `cfa: <CS> GetRoute FAILED (runway <Name>/<PhysicalName>): …` and bails cleanly instead of letting the exception escape into the game tick.
- Aids used at runtime are `FindObjectsOfType<Aircraft3D>` (lookup by `Source.CallSign`), a cached VContainer `LifetimeScope` scan for `AirwayRouteService`, and a per-command VContainer scan for `RadioChannelManager` / `RadioSystem` (the jurisdiction handoff's tower-channel resolution).

## Rollback

Delete `BepInEx\plugins\AC27Approach.dll` and restart the game. Without the plugin both frame types are inert: a `!` SelectAircraft frame is a normal select miss, and a `0x00E7` frame is one `UnknownCommand` log line.

## Build

- SDK: net6.0 (built with SDK 7.0.410+), `LangVersion 10`, references pinned to `<GameDir>\BepInEx\core\` and `BepInEx\interop\` (see `AC27Approach.csproj` — `GameDir` property points at the Playtest install).
- Interop gotchas fixed at build time are documented in `docs/bepinex-aircraft-override-report.md` §6.4 ("Build-verified deltas").
- Design: `docs/bepinex-aircraft-override-report.md` (input surfaces §5, API design §4, verification checklist §8).
